import React from "react";

const message = () => {
  return (
    <div className="text-text-froggie-green text-[15px] text-wrap">
      LeiaBot: user was banned for sending server invites user bubby.20
    </div>
  );
};

export default message;
